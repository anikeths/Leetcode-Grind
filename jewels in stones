class Solution:
    def numJewelsInStones(self, jewels: str, stones: str) -> int:
        res=[]
        for j in jewels:
            for s in stones:
                
                if(s==j):
                    print(j,s)
                    res.append(s)

        return len(res)
